<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.dnitinverma.mylibrary">

    <application android:label="@string/app_name"
        android:supportsRtl="true">

        <service
            android:name=".services.ContactMonitorService"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.jhaiho.services.contactMonitorService" />
            </intent-filter>
        </service>

        <service android:name=".services.CopyContactService" >
            <intent-filter>
                <action android:name="com.jhaiho.services.contactserviceClass" />
            </intent-filter>
        </service>

    </application>

</manifest>
